# 문서 포맷 기준

저장소의 모든 Markdown 문서는 이 기준을 따라요.  
문서를 추가하거나 고칠 때, 형식을 정리할 때 이 문서를 기준으로 맞춰요.

## 작성 원칙

- 제품 기능과 명령은 공식 자료와 지금 설치 환경으로 확인한 내용만 적어요. 확인되지 않은 내용은 `확인 필요`로 표시해요.
- 같은 내용은 한곳에만 적고 나머지는 링크해요.
- 문서에 이력관리 섹션을 두지 않아요.
- Playbook에는 Skill 호출 표기를 쓰지 않아요. 호출 표기는 플랫폼 문서가 관리해요.
- 새 제품을 추가해도 작업 흐름은 복사하지 않고 Playbook을 링크해요.

## 문체

- 본문은 한국어 해요체로 작성해요.
- 짧고 명확한 문장만 남기고, 없어도 의미가 같은 단어는 빼요.
- 문장으로 끝나는 목록 항목도 해요체로 써요. `~않음`, `~사용.` 같은 명사형 종결은 쓰지 않아요.
- 표 칸과 목록의 짧은 명사구는 문장으로 바꾸지 않아요. 명사구 뒤에 설명 문장이 이어지면 명사구도 `~이에요`로 끝내요.
- 코드명, 파일명, 명령, API명과 제품명은 원문 표기를 유지해요.
- 에이전트 지침 파일(`AGENTS.md`)과 코드 블록 안의 에이전트 지침 예제(`AGENTS.md`, `CLAUDE.md`, Skill 본문)는 한다체를 유지해요.

## 줄바꿈

- 문단은 한 줄에 한 문장만 둬요.
- 다음 줄로 이어지는 문단 줄은 끝에 공백 두 개를 넣어요.
- 한 문장은 A4 한 줄 너비까지 붙여 두고, 그보다 길 때만 의미 단위로 나눠요.
- 목록 항목 안의 짧은 두 문장은 한 줄에 둘 수 있어요.
- 목록 항목 끝에는 공백을 두지 않아요.
- 표 칸 안에서 `<br>`로 줄을 바꾸지 않아요.

## 요소 형식

### 제목

- `#` 제목은 문서마다 하나만 둬요.
- 절차를 순서대로 따라가는 문서는 `## 1. 제목`처럼 번호를 붙여요. 그 밖의 문서는 번호 없이 써요.
- `reference/` 문서의 `#` 제목은 `# 01. 제목` 형식으로 써요. 난이도는 `# 01. [초급] 제목`처럼 제목 앞에 붙여요.

### 머리말

대상과 범위를 먼저 밝혀야 하는 문서는 제목 바로 아래에 인용 블록을 둬요.

```markdown
> 대상: 이 문서를 읽는 사람 또는 시스템  
> 범위: 다루는 내용  
> 제외: 다루지 않는 내용과 그 내용을 다루는 문서 링크
```

- 영역 README는 `대상`, `범위`, `제외`를 써요.
- 영역 가이드 문서는 `대상`, `목적`, `원칙`을 써요.
- 필요 없는 줄은 빼요.

### 표

- 구분선은 `| --- | --- |` 형식으로 써요. 정렬이 필요하면 `| ---: |`처럼 콜론만 더해요.
- 칸 앞뒤에 공백 한 칸을 둬요.

### 코드 블록

- 언어를 명시해요. 셸 명령은 `bash`, 입력창 요청은 `text`, 설정 파일은 `toml`·`json`·`yaml`처럼 파일 형식을 써요.
- 코드 블록 안은 형식 정리 대상에서 빼요.

### 링크

- 내부 링크는 저장소 기준 상대 경로를 사용해요.
- 링크 텍스트는 대상 문서의 제목이나 역할을 써요.

## 파일과 폴더

| 대상 | 위치와 이름 |
| --- | --- |
| 영역 가이드 | `<영역>/<영역 소문자>_NN_<주제>.md`, 주제는 snake_case |
| Playbook | `Playbooks/NN-<주제>.md`, 주제는 kebab-case |
| 플랫폼 문서 | `Platforms/<제품>/`의 `README.md`, `setup.md`, `skills.md`, `commands.md`, `workflows.md`, `extensions.md` |
| 플랫폼 고유 기능 | `Platforms/<제품>/<기능>.md` |
| 제품 전용 학습 자료 | `Platforms/<제품>/reference/NN-<주제>.md` |

문서를 추가, 이동하거나 이름을 바꾸면 `README.md`, `index.md`와 해당 영역 `README.md`의 링크를 함께 갱신해요.

## 문서 유형별 구조

섹션은 아래 순서로 둬요.  
`(필요 시)` 표시가 없는 섹션은 빠뜨리지 않아요.

### 영역 README

Git, Python, Macos, IntelliJ처럼 주제 영역의 시작 문서예요.

1. `# <영역> 가이드 제목`
2. 머리말 인용 블록 (필요 시)
3. 영역 소개 1~2문장
4. `## 학습 순서`: `| 순서 | 문서 | 익혀야 할 내용 |` 표
5. `## 환경 준비` (필요 시)
6. `## 공통 원칙` (필요 시)
7. 영역 고유 섹션: `## 매일 사용하는 작업 흐름`, `## 상태 확인 명령` 등 (필요 시)
8. `## 완료 기준` (필요 시)
9. `## 팀에서 정할 것` (필요 시)
10. `## 함께 사용하는 문서` (필요 시)
11. `## 앞으로 추가할 문서` (필요 시)

### 영역 가이드 문서

1. `# 제목`
2. 머리말 인용 블록 (필요 시)
3. 문서 소개 1~3문장
4. 본문 섹션
5. `## 다음 단계` 또는 `## 공식 문서` (필요 시)

### Playbook

1. `# 제목`
2. 결과를 설명하는 1문장
3. `## 흐름`: `text` 코드 블록
4. `## 단계별로 확정할 것`: 단계마다 `### <Skill 이름>` 또는 `### <Skill 이름> — <단계>`
5. `## 다음 단계로 넘길 것`
6. `## 이 Playbook을 벗어나는 경우`
7. `## 실행`

### 플랫폼 README

1. `# <제품> 가이드`
2. 소개: 할 일과 이 가이드가 다루는 범위, [Playbook](./Playbooks/README.md) 링크
3. `## 바로 시작`: 실행 순서와 Skill 호출 예제
4. `## 무엇을 하려나요?`: `| 목적 | Skill | 시작 예제 |` 표
5. 제품 고유 섹션 (필요 시)
6. `## 필요한 문서`: `| 알고 싶은 것 | 문서 |` 표
7. `## 사용 기준`
8. `## 심화 학습 자료`: `reference/` 문서 표 (필요 시)

### 플랫폼 하위 문서

`setup.md`, `skills.md`, `commands.md`, `workflows.md`, `extensions.md`와 고유 기능 문서예요.

1. `# <제품> <문서 역할>`
2. 문서 소개 1~2문장
3. 본문 섹션
4. `## 확인 기준` (필요 시)
5. `## 공식 문서` (필요 시)

### reference 문서

1. `# NN. 제목`
2. 문서 소개와 기준 버전
3. 본문 섹션: 번호는 [제목](#제목) 규칙을 따라요

## 포맷 확인 목록

문서를 고친 뒤 아래를 확인해요.

- [ ] 문체가 해요체이고, 한다체와 명사형 종결이 코드 블록 밖에 없어요.
- [ ] 문단 줄마다 한 문장이고, 이어지는 줄 끝에 공백 두 개가 있어요.
- [ ] 목록 끝 공백과 표 칸 `<br>`이 없어요.
- [ ] 표 구분선이 `| --- |` 형식이에요.
- [ ] 코드 블록에 언어가 있어요.
- [ ] 문서 유형별 섹션 이름과 순서가 이 기준과 같아요.
- [ ] 파일 이름이 위치 규칙과 같고, 관련 링크를 모두 갱신했어요.
